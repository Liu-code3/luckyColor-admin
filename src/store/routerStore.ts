import { defineStore } from 'pinia';

export const routerStore = defineStore({
  id: 'router',
  state: () => ({

  }),
  getters: {},
  actions: {

  }
});

export const useGlobalStore = routerStore;
